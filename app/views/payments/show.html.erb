 <% if current_user.admin %>
  <%= render "/shared/navpath", parent: :payments, parent_path: payments_path, child: "##{@payment.id}" %>
<% end %> 
<br>
<%= render @payment %>
<hr>
<%= render "history" %>
<br>
<div style="text-align:center">
 <% unless current_user.admin %>
  <%= link_to "Return", book_user_path(@payment.book_user_id), class:"btn btn-outline-primary" %>
 <% end %>
</div>
<!--
<div>
  <% if can? :update, @payment %>
    <%#= link_to "Edit this payment", edit_payment_path(@payment) %> |
  <% end %>
  <%#= link_to "Back to payments", payments_path %>
  <% if can? :destroy, @payment %>
    <%#= button_to "Destroy this payment", @payment, method: :delete %>
  <% end %>
</div>-->