<%= bootstrap_form_with(model: genre, layout: :horizontal) do |form| %>
  <% if genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(genre.errors.count, "error") %> prohibited this genre from being saved:</h2>

      <ul>
        <% genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :category %>
  </div>
  <br>
  <div>
    <%= form.text_area :description %>
  </div>
  </div>
  <br>
  <div style="text-align:center">
    <%= link_to :Return, @genre, class:"btn btn-outline-primary" %>
    <%= form.primary :Submit %>
  </div>
<% end %>
